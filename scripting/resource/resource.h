#pragma once

enum ResourceStatus : uint8_t
{
	ResourceResult_Stopped,
	ResourceResult_Running,
};

enum ResourceAction : uint8_t
{
	ResourceResult_Stop,
	ResourceResult_Start,
	ResourceResult_Restart,
};

class Resource
{
private:

public:
	
};